<!DOCTYPE html>
<html>
  <head>
    <title> Martin Arce | Web Developer </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="menu-mobile
                fixed right-full top-0 z-30
                w-full h-screen
                bg-amber-300
                bg-green-300
                flex flex-col gap-8 justify-between py-12
                text-4xl font-gray-800
                ">
      <div class="flex flex-col items-center justify-center gap-4 py-8">
        <%= link_to 'HOME', root_path %>
        <%= link_to 'WORK', root_path %>
        <%= link_to 'RESUME', root_path %>
        <%= link_to 'CONTACT', root_path %>
      </div>
      <hr class="mt-auto border-0 bg-gray-800 min-h-[.25px]" >
      <div class="flex gap-4 text-sm fonf-gray-800 justify-center items-center py-4">
          <%= link_to 'EN', root_path(locale: :en) %> |
          <%= link_to 'ES', root_path(locale: :es) %>
      </div>
    </div>

      <%= render 'layouts/navbar' %>

    <%= yield %>
    
     <%# <div class="overlay-bg">
      <div class="overlay"></div>
    </div> %>
  </body>
</html>

